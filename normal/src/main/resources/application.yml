server:
  port: 8001
spring:
  application:
    name: nacos-normal-provider
  cloud:
    nacos:
      discovery:
        server-addr: * #配置Nacos地址
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/haruharu?serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  redis:
    host: *
    port: 6379
    password: *
    database: 1
#    key:
#      prefix:
#        product: "product:productId:"


pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  page-size-zero: true
  params: count=contSql


mybatis-plus:
  # xml扫描，多个目录用逗号或者分号分隔（告诉 Mapper 所对应的 XML 文件位置）
  mapper-locations: classpath:com/ryota/normal/mapper/xml/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
